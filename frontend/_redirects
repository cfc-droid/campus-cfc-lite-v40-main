# Redirigir solo rutas de módulos
/modules/*      /index.html   200

# Mantener acceso normal a los assets JS, CSS, etc.
# (usa coincidencia exacta de carpeta con el comodín correcto)
#/frontend/*     /frontend/:splat   200
/frontend/*      /frontend/:splat   200
/js/*            /js/:splat         200
/css/*           /css/:splat        200
/img/*           /img/:splat        200

# Redirección general para SPA
/*               /index.html   200
